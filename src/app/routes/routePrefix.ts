// Path prefixes used for sidebar active-state matching

export const enum RoutePrefix {
  Components = '/dashboard/components',
  Forms = '/dashboard/forms',
  Products = '/dashboard/products',
}
