/** Test ID prefix for all integration page elements. */
export const INTEGRATION_TEST_PREFIX = 'admin-integrations';

/** Label map for integration categories (i18n keys). */
export const CATEGORY_I18N_KEYS: Record<string, string> = {
  communication: 'integrations.categories.communication',
  storage: 'integrations.categories.storage',
  payment: 'integrations.categories.payment',
  analytics: 'integrations.categories.analytics',
  auth: 'integrations.categories.auth',
  monitoring: 'integrations.categories.monitoring',
};

/** Value representing "all categories" in the filter dropdown. */
export const ALL_CATEGORIES_VALUE = 'all';
