/* eslint-disable no-magic-numbers -- Sequential enum indices are inherently numeric */

/** Tab indices for the settings page. */
export const enum SettingsTab {
  General = 0,
  Security = 1,
  Email = 2,
  Notifications = 3,
  Maintenance = 4,
}
