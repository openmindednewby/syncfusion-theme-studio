/** Default canvas width for the diagram component. */
export const CANVAS_WIDTH = '100%';

/** Default canvas height for the diagram component. */
export const CANVAS_HEIGHT = '600px';

/** Grid interval size in pixels for the snap grid. */
export const GRID_INTERVAL = 10;

/** Default zoom factor for zoom in/out operations. */
export const ZOOM_FACTOR = 0.2;

/** Palette symbol dimensions (width). */
export const PALETTE_SYMBOL_WIDTH = 50;

/** Palette symbol dimensions (height). */
export const PALETTE_SYMBOL_HEIGHT = 50;

/** Palette container width. */
export const PALETTE_WIDTH = '100%';

/** Palette container height. */
export const PALETTE_HEIGHT = '100%';
