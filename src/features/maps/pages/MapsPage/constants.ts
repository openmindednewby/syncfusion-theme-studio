/** Zoom settings for the Syncfusion Maps component. */
export const MAP_ZOOM_FACTOR = 2;
export const MAP_MIN_ZOOM = 1;
export const MAP_MAX_ZOOM = 10;

/** Bubble size range for employee count visualization. */
export const BUBBLE_MIN_SIZE = 10;
export const BUBBLE_MAX_SIZE = 40;

/** Marker size in pixels. */
export const MARKER_SIZE = 12;

/** Panel width in Tailwind class. */
export const PANEL_WIDTH_CLASS = 'w-96';

/** Map color for land areas. */
export const MAP_FILL_COLOR = 'var(--color-surface-elevated)';
export const MAP_BORDER_COLOR = 'var(--color-border)';

/** Marker colors. */
export const MARKER_FILL = 'var(--color-primary)';
export const MARKER_SELECTED_FILL = 'var(--color-accent)';
