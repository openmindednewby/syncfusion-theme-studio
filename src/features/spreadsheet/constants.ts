/** Height of the spreadsheet component, accounting for page header. */
export const SPREADSHEET_HEIGHT = 'calc(100vh - 180px)';

/** Number of sample sheets available. */
export const SAMPLE_SHEET_COUNT = 3;
