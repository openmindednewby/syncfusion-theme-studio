// Shared const enum keys for data display component default configs

export const enum DataDisplayKeys {
  Tabs = 'tabs',
  Timeline = 'timeline',
  Avatar = 'avatar',
  ProgressBar = 'progressBar',
  Tooltip = 'tooltip',
}
