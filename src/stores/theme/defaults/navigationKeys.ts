// Shared const enum keys for navigation component default configs

export const enum NavigationKeys {
  Accordion = 'accordion',
  Toolbar = 'toolbar',
  Menu = 'menu',
  Breadcrumb = 'breadcrumb',
}
