// Floating action button (FAB) type definitions

export interface FabConfig {
  background: string;
  backgroundHover: string;
  backgroundActive: string;
  iconColor: string;
  textColor: string;
  borderRadius: string;
  size: string;
  iconSize: string;
  shadow: string;
  shadowHover: string;
  disabledBackground: string;
  disabledOpacity: string;
  transitionDuration: string;
}
