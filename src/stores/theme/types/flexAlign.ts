// Flex align enum for flexbox layout configuration

export const enum FlexAlign {
  FlexStart = 'flex-start',
  Center = 'center',
  FlexEnd = 'flex-end',
  Stretch = 'stretch',
  Baseline = 'baseline',
}
