// Search panel component type definitions

export interface SearchPanelConfig {
  background: string;
  borderColor: string;
  gap: string;
  padding: string;
  filterRowGap: string;
  labelColor: string;
  labelFontSize: string;
  searchWidth: string;
  filterWidth: string;
  borderRadius: string;
  labelFontWeight: string;
}
