// Toggle/Switch component type definitions

export interface ToggleConfig {
  trackActiveBackground: string;
  trackInactiveBackground: string;
  thumbBackground: string;
  thumbShadow: string;
  focusRingColor: string;
  disabledOpacity: string;
  trackBorderRadius: string;
  trackWidth: string;
  trackHeight: string;
  thumbSize: string;
}
