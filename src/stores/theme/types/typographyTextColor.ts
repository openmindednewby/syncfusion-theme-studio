// Typography text color keys - references mode-aware CSS variables
const enum TypographyTextColor {
  Primary = 'primary',
  Secondary = 'secondary',
  Muted = 'muted',
}

export { TypographyTextColor };
